# Google Analytics measurement id. PUBLIC_ vars are inlined into the client bundle.
PUBLIC_GA_MEASUREMENT_ID=

# Readwise access token, from https://readwise.io/access_token
# Read at build time only: it must not carry a PUBLIC_ prefix, or it would be
# shipped to the browser. In CI it comes from the repository secret of the same name.
READWISE_ACCESS_TOKEN=
